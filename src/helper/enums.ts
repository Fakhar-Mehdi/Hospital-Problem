export enum ROUTES {
  PATIENT_PATH = "/api/patient",
  APPOINTMENT_PATH = "/api/appointment",
  HOSPITAL_PATH = "/api",
}

export enum NO_OF_LEGS {
  FOUR = "4",
  TWO = "2",
  BIRD = "1",
  REPTILE = "0",
}

export enum CURRENCY {
  USD = "USD",
  EUR = "EUR",
  BTC = "BTC",
}
